```bash
date +%Y-%m-%d-%s
curl -X PUT -d @- http://localhost:12330/localhost/apt/test/1.2
xbacklight -set 100
mount | grep '/work' || mount -L WORK /work -o fmask=117,dmask=007,uid=x
openssl pkcs12 -in butpsuite.pk12 -out burpsuite.pem -nodes -clcerts  # pk12 -> pem
wget --recursive --no-clobber  --page-requisites --html-extension --convert-links -e robots=off --restrict-file-names=windows --domains $1 $@
```
